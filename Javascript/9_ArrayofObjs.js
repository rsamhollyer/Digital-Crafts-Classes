// //1
// let ships = [
// 	{ name: "One", topSpeed: 100, accel: 20, cargo: 100 },
// 	{ name: "Two", topSpeed: 200, accel: 40, cargo: 50 },
// 	{ name: "Three", topSpeed: 400, accel: 80, cargo: 25 },
// ];

// console.log(ships[1].name, ":", ships[1].topSpeed);

// ships.forEach((ship) => {
// 	for (att in ship) {
// 		if (!ship.hasOwnProperty(att)) continue;
// 		console.log(att, ":", ship[att]);
// 	}
// });

// let fastest = ships.sort((a, b) => (a.topSpeed > b.topSpeed ? -1 : 1));
// console.log(fastest[0]);
